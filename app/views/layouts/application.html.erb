<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "PNM" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <!-- Icons -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <!-- Bootstrap Icons CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <!-- Styles & Scripts -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      /* Ensure flash messages appear below the navbar */
      .alert.flash-notification {
        position: fixed !important;
        top: 56px !important; /* Position below navbar (Bootstrap navbar default height) */
        left: 0 !important;
        right: 0 !important;
        width: 100% !important;
        z-index: 1030 !important; /* Below navbar (1030) but above content */
        margin: 0 !important;
        border-radius: 0 !important;
      }
      
      /* Add padding to body when flash notification is present */
      body.has-flash-notification {
        padding-top: 0px !important; /* Navbar height (56px) + notification height (~56px) */
      }
    </style>
  </head>
 <body class="<%= controller_name == 'dashboard_controller' ? 'site-dark' : '' %><%= ' has-flash-notification' if notice.present? || alert.present? %>">
    <!-- :arrows_anticlockwise: Conditional Navbar -->
    <% if user_signed_in? && current_user.role == "admin" %>
    <%= render "shared/admin_navbar" %>
    <% elsif user_signed_in? %>
    <%= render "shared/navbar" %>
    <% end %>
    <!-- :bell: Flash Messages -->
    <% if notice.present? %>
      <div class="alert alert-success text-center mb-0 flash-notification">
        <%= notice %>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="alert alert-danger text-center mb-0 flash-notification">
        <%= alert %>
      </div>
    <% end %>
    <!-- :page_facing_up: Page Content -->
    <%= yield %>
  </body>
</html>